---
// Sección "Nuestra Diferencia" con tarjetas animadas
const features = [
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"></path>`,
    title: "Variedad de Cortes Premium",
    description: "Selección exclusiva de los mejores cortes de carne, desde picanha hasta costillas, preparados con maestría."
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.879 16.121A3 3 0 1012.015 11L11 14H9c0 .768.293 1.536.879 2.121z"></path>`,
    title: "Técnica de Parrilla Precisa",
    description: "Nuestros parrilleros dominan el arte del fuego perfecto, garantizando el punto exacto en cada corte."
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>`,
    title: "Servicio Continuo en Mesa",
    description: "Experiencia auténtica de rodizio con servicio personalizado que lleva los cortes directamente a tu mesa."
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>`,
    title: "Ambiente Auténtico",
    description: "Espacio diseñado para capturar la esencia del rodizio brasileño-argentino con toques modernos."
  }
];
---

<section class="py-24 bg-gradient-to-b from-[var(--color-cream)] to-white">
  <div class="max-w-7xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="mb-4">
        Nuestra <span class="text-gradient">Diferencia</span>
      </h2>
      <p class="text-xl text-[var(--color-carbon)]/70 max-w-2xl mx-auto">
        Más que un restaurante, una experiencia gastronómica que celebra la tradición de la parrilla
      </p>
    </div>

    <!-- Features Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {features.map((feature, index) => (
        <div 
          class="feature-card card-hover bg-white rounded-2xl p-8 text-center shadow-lg border border-[var(--color-wood)]/20"
          style={`animation-delay: ${index * 0.1}s`}
        >
          <!-- Icon -->
          <div class="mb-6 flex justify-center">
            <div class="w-20 h-20 bg-gradient-to-br from-[var(--color-terracotta)] to-[var(--color-gold)] rounded-full flex items-center justify-center shadow-lg">
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" set:html={feature.icon}>
              </svg>
            </div>
          </div>

          <!-- Content -->
          <h3 class="text-xl font-bold mb-3 text-[var(--color-carbon)]">
            {feature.title}
          </h3>
          <p class="text-[var(--color-carbon)]/70 leading-relaxed">
            {feature.description}
          </p>

          <!-- Decorative element -->
          <div class="mt-6 h-1 w-16 bg-gradient-to-r from-[var(--color-terracotta)] to-[var(--color-gold)] mx-auto rounded-full"></div>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <div class="text-center mt-16">
      <a href="/menu" class="btn-primary">
        Descubre Nuestro Menú
      </a>
    </div>
  </div>
</section>

<style>
  .feature-card {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .feature-card:hover .w-20 {
    animation: grill-pulse 2s ease-in-out infinite;
  }
</style>
