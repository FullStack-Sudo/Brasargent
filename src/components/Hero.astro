---
// Hero Section con video de fondo y overlay de brasas
---

<section
  class="relative h-screen flex items-center justify-center overflow-hidden brasas-overlay"
>
  <!-- Background Image/Video Placeholder -->
  <div class="absolute inset-0 z-0">
    <img
      src="/hero-bg.jpg"
      alt="Parrilla Brasargent"
      class="w-full h-full object-cover"
      id="hero-bg"
    />
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-6 max-w-5xl mx-auto">
    <!-- Icono de cuchillo estilizado -->
    <div class="mb-8 flex justify-center">
      <div class="relative">
        <svg
          class="w-20 h-20 text-[var(--color-gold)] animate-float"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="1.5"
            d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z"
          ></path>
        </svg>
        <div
          class="absolute inset-0 blur-xl bg-[var(--color-gold)] opacity-50 animate-pulse"
        >
        </div>
      </div>
    </div>

    <!-- Título Principal -->
    <h1 class="text-white mb-6 leading-tight">
      El mejor <span class="text-gradient">Rodizio</span><br />del País
    </h1>

    <!-- Subtítulo -->
    <p
      class="text-white/90 text-xl md:text-2xl mb-4 font-light max-w-3xl mx-auto"
    >
      Parrilla de alta calidad con técnica precisa
    </p>
    <p class="text-[var(--color-gold)] text-lg md:text-xl mb-12 font-medium">
      Variedad de cortes premium • Servicio continuo en mesa
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a href="tel:78555886" class="btn-primary">
        <span class="flex items-center gap-2">
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
            ></path>
          </svg>
          Reservar Mesa
        </span>
      </a>
      <a href="/menu" class="btn-secondary"> Ver Menú </a>
    </div>

    <!-- Scroll Indicator -->
    <div
      class="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-bounce"
    >
      <svg
        class="w-6 h-6 text-white/70"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </div>

  <!-- Particles effect (opcional) -->
  <div class="absolute inset-0 pointer-events-none">
    <div
      class="absolute top-1/4 left-1/4 w-2 h-2 bg-[var(--color-gold)] rounded-full opacity-50 animate-float"
    >
    </div>
    <div
      class="absolute top-1/3 right-1/4 w-1 h-1 bg-[var(--color-gold)] rounded-full opacity-30 animate-float"
      style="animation-delay: 1s;"
    >
    </div>
    <div
      class="absolute bottom-1/3 left-1/3 w-1.5 h-1.5 bg-[var(--color-gold)] rounded-full opacity-40 animate-float"
      style="animation-delay: 2s;"
    >
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>
