---
// Componente de Sucursales con diseño futurista
const sucursales = [
  //nueva sucursal
  {
    name: "Churrasqueria",
    rating: 4.3,
    reviews: "1.2K",
    address: "Sucursal Beni, Av. casi - 5to anillo ",
    days: "",
    hours:
      "Lunes a Sabado 11:30 am - 15:00 pm / 18:30 pm - 22:00 pm / Domingo 11:30 am - 15:00 pm",
    image: "/img/sucursales/beni.jpeg",
    whatsapp: "59178007700", // Reemplazar con el número real
    mapUrl: "https://maps.app.goo.gl/Ee8GbY1Ufdon1KC88",
    type: "Churrasquería",
    status: "",
    footerLogo: "/img/logo/LOGO1SF.png",
  },
  {
    name: "Fast Grill",
    rating: 4.5,
    reviews: "1.3K",
    price: "",
    address: "C. 12 Oeste",
    hours: "Abierto al publico desde las 12:00 PM, hasta las 22:00 PM",
    image: "/img/logo/Sucursal2.jpeg",
    featured: true,
    whatsapp: "59178557004", // Reemplazar con el número real
    type: "Rodizio Tradicional",
    status: "",
    mapUrl: "https://maps.app.goo.gl/JCHxQszooKH5mJ9S8?g_st=aw",
    footerLogo: "/img/logo/LOGO2SF.png",
  },
  {
    name: "Rodizio",
    rating: 3.9,
    reviews: "100",
    address: "Las Brisas",
    hours:
      "Lunes a Sabado 11:30 am - 15:30 pm / 18:30 pm - 22:00 pm / Domingo 11:30 am - 15:30 pm",
    image: "/img/sucursales/RODIZIO.webp",
    whatsapp: "59178555509", // Reemplazar con el número real
    type: "Restaurante",
    status: "",
    testimonial: "",
    mapUrl: "https://maps.app.goo.gl/xs1BckNvmFdErVG2A",
    footerLogo: "/img/logo/LOGO3SF.png",
  },
];
---

<section
  id="sucursales"
  class="relative pb-24 overflow-hidden"
  style="padding-top: 250px; background-image: linear-gradient(135deg, rgba(159, 159, 159, 0.46) 0%, rgba(159, 159, 159, 0.46) 14.286%,rgba(165, 165, 165, 0.46) 14.286%, rgba(165, 165, 165, 0.46) 28.572%,rgba(171, 171, 171, 0.46) 28.572%, rgba(171, 171, 171, 0.46) 42.858%,rgba(178, 178, 178, 0.46) 42.858%, rgba(178, 178, 178, 0.46) 57.144%,rgba(184, 184, 184, 0.46) 57.144%, rgba(184, 184, 184, 0.46) 71.43%,rgba(190, 190, 190, 0.46) 71.43%, rgba(190, 190, 190, 0.46) 85.716%,rgba(196, 196, 196, 0.46) 85.716%, rgba(196, 196, 196, 0.46) 100.002%),linear-gradient(45deg, rgb(252, 252, 252) 0%, rgb(252, 252, 252) 14.286%,rgb(246, 246, 246) 14.286%, rgb(246, 246, 246) 28.572%,rgb(241, 241, 241) 28.572%, rgb(241, 241, 241) 42.858%,rgb(235, 235, 235) 42.858%, rgb(235, 235, 235) 57.144%,rgb(229, 229, 229) 57.144%, rgb(229, 229, 229) 71.43%,rgb(224, 224, 224) 71.43%, rgb(224, 224, 224) 85.716%,rgb(218, 218, 218) 85.716%, rgb(218, 218, 218) 100.002%);"
>
  <!-- Efectos de fondo decorativos (reducidos) -->
  <div class="absolute inset-0 opacity-5">
    <div
      class="absolute top-50 left-10 w-96 h-96 bg-[#d4af37] rounded-full filter blur-[120px] animate-pulse"
    >
    </div>
    <div
      class="absolute bottom-20 right-10 w-96 h-96 bg-[#9e2a2b] rounded-full filter blur-[120px] animate-pulse"
      style="animation-delay: 1s;"
    >
    </div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-16 space-y-4" data-aos="fade-up">
      <h2
        class="text-5xl md:text-6xl font-bold text-white drop-shadow-[0_2px_8px_rgba(0,0,0,0.8)]"
      >
        <span
          class="bg-gradient-to-r from-[#d4af37] via-[#f4d03f] to-[#d4af37] bg-clip-text text-transparent animate-gradient drop-shadow-[0_2px_8px_rgba(0,0,0,0.8)]"
        >
          Encuentra tu Brasargent más cercano
        </span>
        <br />
        <br />
        <br />
      </h2>
    </div>
    <div class="py-12"></div>
    <!-- Grid de Sucursales - Grid cuadrado centrado -->
    <div class="flex justify-center">
      <div
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto"
      >
        {
          sucursales.map((sucursal, index) => (
            <div
              class="group relative w-full min-h-[600px] bg-white rounded-3xl shadow-[0_10px_50px_rgba(0,0,0,0.1)] hover:shadow-[0_20px_70px_rgba(0,0,0,0.2)] border border-gray-100 transition-all duration-500 flex flex-col pb-6"
              data-aos="fade-up"
              data-aos-delay={index * 100}
            >
              {/* Imagen de la sucursal */}
              <div class="relative h-64 overflow-hidden rounded-t-2xl">
                <img
                  src={sucursal.image}
                  alt={sucursal.name}
                  class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                />
                {/* Badge de destacado */}
                {sucursal.featured && (
                  <div class="absolute top-4 right-4 bg-gradient-to-r from-[#d4af37] via-[#f4d03f] to-[#d4af37] text-black text-[10px] font-black px-4 py-2 rounded-full shadow-xl z-10 tracking-widest animate-pulse">
                    ⭐ DESTACADO
                  </div>
                )}
                {/* Overlay oscuro en la imagen */}
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent" />
              </div>

              {/* Contenido de la card - con grow para empujar botones abajo */}
              <div class="p-8 flex flex-col grow items-center">
                {/* Nombre y rating */}
                <h3 class="text-3xl font-black text-gray-900 mb-4 text-center tracking-tight group-hover:text-[#9e2a2b] transition-colors duration-300">
                  {sucursal.name}
                </h3>

                <div class="flex items-center justify-center gap-3 mb-4 bg-gray-50 px-4 py-2 rounded-full">
                  <div class="flex items-center gap-1.5">
                    <span class="text-[#d4af37] text-lg">★</span>
                    <span class="text-gray-900 font-black">
                      {sucursal.rating}
                    </span>
                    <span class="text-gray-400 text-xs font-bold">
                      ({sucursal.reviews} reseñas)
                    </span>
                  </div>
                  {sucursal.price && (
                    <>
                      <span class="w-1 h-1 bg-gray-300 rounded-full" />
                      <span class="text-[#d4af37] font-black">
                        {sucursal.price}
                      </span>
                    </>
                  )}
                </div>

                {/* Tipo */}
                <p class="text-[#9e2a2b] text-xs font-black uppercase tracking-[0.2em] mb-6 text-center">
                  {sucursal.type}
                </p>

                {/* Dirección */}
                <div class="flex flex-col items-center gap-2 mb-5 text-center px-4">
                  <div class="w-10 h-10 flex items-center justify-center bg-[#d4af37]/10 rounded-full mb-1">
                    <svg
                      class="w-5 h-5 text-[#d4af37]"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2.5"
                        d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                      />
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2.5"
                        d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"
                      />
                    </svg>
                  </div>
                  <p class="text-gray-600 text-sm font-medium leading-relaxed">
                    {sucursal.address}
                  </p>
                </div>

                {/* Horario */}
                {sucursal.hours && (
                  <div class="flex flex-col items-center gap-2 mb-6 text-center">
                    <div class="w-10 h-10 flex items-center justify-center bg-[#9e2a2b]/10 rounded-full mb-1">
                      <svg
                        class="w-5 h-5 text-[#9e2a2b]"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2.5"
                          d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                        />
                      </svg>
                    </div>
                    <div class="text-sm font-medium px-4">
                      {sucursal.status && (
                        <span class="text-red-500 font-black block mb-1">
                          {sucursal.status}
                        </span>
                      )}
                      <span class="text-gray-500 leading-relaxed italic">
                        {sucursal.hours}
                      </span>
                    </div>
                  </div>
                )}

                {/* Testimonial */}
                {sucursal.testimonial && (
                  <div class="relative px-8 mb-6">
                    <span class="absolute top-0 left-4 text-4xl text-gray-200 font-serif">
                      “
                    </span>
                    <p class="text-gray-500 italic text-sm text-center relative z-10 line-clamp-2">
                      {sucursal.testimonial}
                    </p>
                    <span class="absolute bottom-0 right-4 text-4xl text-gray-200 font-serif leading-none">
                      ”
                    </span>
                  </div>
                )}

                {/* Spacer superior para centrar botones */}
                <div class="grow" />

                {/* Botones de Acción - Circulares - Centrados */}
                <div class="flex flex-row flex-nowrap gap-4 md:gap-6 items-center justify-center py-6">
                  {/* Botón Ver Ubicación - Verde Google (Circular) */}
                  <a
                    href={
                      sucursal.mapUrl ||
                      `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(sucursal.name + " " + sucursal.address)}`
                    }
                    target="_blank"
                    rel="noopener noreferrer"
                    class="w-16 h-16 flex items-center justify-center bg-[#34A853] text-white rounded-full hover:bg-[#2d9348] hover:shadow-2xl hover:shadow-[#34A853]/60 transition-all duration-300 hover:scale-110 group"
                    title="Ver ubicación en Google Maps"
                  >
                    <svg
                      class="w-7 h-7 group-hover:scale-110 transition-transform duration-300"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                      />
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"
                      />
                    </svg>
                  </a>

                  {/* Botón Ver Menú - Rojo Vino (Circular) - Abre Modal */}
                  <button
                    type="button"
                    data-modal-open="menu-modal"
                    data-sucursal-name={sucursal.name}
                    class="w-16 h-16 flex items-center justify-center bg-[#9e2a2b] text-white rounded-full hover:bg-[#8a2426] hover:shadow-2xl hover:shadow-[#9e2a2b]/60 transition-all duration-300 hover:scale-110 group"
                    title="Ver nuestro menú"
                  >
                    <svg
                      class="w-7 h-7 group-hover:scale-110 transition-transform duration-300"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"
                      />
                    </svg>
                  </button>

                  {/* Botón WhatsApp - Verde WhatsApp (Circular) */}
                  <a
                    href={`https://wa.me/${sucursal.whatsapp}?text=${encodeURIComponent(`Hola, me gustaría hacer una consulta sobre ${sucursal.name}`)}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="w-16 h-16 flex items-center justify-center bg-[#25D366] text-white rounded-full hover:bg-[#20ba5a] hover:shadow-2xl hover:shadow-[#25D366]/60 transition-all duration-300 hover:scale-110 group"
                    title="Contactar por WhatsApp"
                  >
                    <svg
                      class="w-7 h-7 group-hover:scale-110 transition-transform duration-300"
                      fill="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z" />
                    </svg>
                  </a>

                  <!-- Botón Reservar - Dorado (Circular/Pill) -->
                  {sucursal.name !== "Fast Grill" && (
                    <button 
                        class="btn-reservar w-16 h-16 flex items-center justify-center bg-[#d4af37] text-white rounded-full hover:bg-[#b8941f] hover:shadow-2xl hover:shadow-[#d4af37]/60 transition-all duration-300 hover:scale-110 group" 
                        onclick={`abrirModalReserva(${index + 1}, '${sucursal.name}')`}
                        data-sucursal-id={index + 1}
                        title="Reservar mesa"
                    >
                        <svg class="w-7 h-7 group-hover:scale-110 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                        </svg>
                    </button>
                  )}
                </div>

                {/* Spacer inferior para centrar botones */}
                <div class="grow"></div>
              </div>
            </div>
          ))
        }
      </div>
    </div>
    <div class="h-32 md:h-48"></div>
    <!-- Espaciador explícito -->
    {/* CTA Final */}
    <div class="mt-20 text-center" data-aos="fade-up" data-aos-delay="200">
      <div
        class="inline-block bg-white/80 backdrop-blur-xl rounded-2xl border border-gray-200 shadow-lg p-8"
      >
        <h3 class="text-2xl font-bold text-gray-900 mb-4">
          ¿No encuentras tu sucursal más cercana?
        </h3>
        <p class="text-gray-600 mb-6">
          Contáctanos y te ayudaremos a encontrar la mejor opción para ti
        </p>
        <a
          href="/#contacto"
          class="inline-block bg-[#9e2a2b] text-white font-semibold px-10 py-4 rounded-lg hover:bg-[#8a2426] hover:shadow-xl hover:shadow-[#9e2a2b]/50 transition-all duration-300 hover:scale-105 border border-[#9e2a2b] hover:border-[#d4af37]"
        >
          Reservar
        </a>
      </div>
    </div>
  </div>
  <div class="py-20"></div>

  {/* Modal del Menú */}
  <div
    id="menu-modal"
    class="fixed inset-0 z-100 hidden items-center justify-center bg-black/60 shadow-2xl"
    data-modal="menu-modal"
  >
    <div
      class="relative max-w-7xl w-full mx-auto bg-white/90 backdrop-blur-md rounded-[40px] border border-gray-200 shadow-5xl overflow-hidden animate-modal-enter transition-all duration-500 bg-cover bg-center bg-no-repeat"
      style="background-image: url('/img/fondo/modalSucursal.svg');"
    >
      <!-- Tech Accents -->
      <div
        class="absolute top-0 left-0 w-12 h-12 border-t-4 border-l-4 border-[#9e2a2b]/40 z-30"
      >
      </div>
      <div
        class="absolute top-0 right-0 w-12 h-12 border-t-4 border-r-4 border-[#9e2a2b]/40 z-30"
      >
      </div>
      <div
        class="absolute bottom-0 left-0 w-12 h-12 border-b-4 border-l-4 border-[#9e2a2b]/40 z-30"
      >
      </div>
      <div
        class="absolute bottom-0 right-0 w-12 h-12 border-b-4 border-r-4 border-[#9e2a2b]/40 z-30"
      >
      </div>

      <!-- Close Button (Direct child for better mobile responsiveness) -->
      <div class="absolute top-6 right-6 z-50">
        <button
          type="button"
          data-modal-close="menu-modal"
          class="w-14 h-14 flex items-center justify-center bg-gray-100 hover:bg-gray-200 text-gray-900 rounded-full transition-all duration-300 hover:scale-110 border border-gray-200 shadow-xl focus:outline-none"
          title="Cerrar menú"
        >
          <svg
            class="w-8 h-8"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            stroke-width="3"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      {/* Header del Modal - Premium Gradient Design */}
      <div
        class="relative bg-white/20 backdrop-blur-sm px-4 py-8 md:py-10 shadow-sm z-20 border-b border-white/10"
      >
        <div class="relative flex items-center justify-center h-16 md:h-20">
          {/* Logo - Absoluto a la Izquierda */}
          <div class="absolute left-0 top-1/2 -translate-y-1/2 ml-2 md:ml-6">
            <img
              src="/img/logo/logoRojo-SF.png"
              alt="Brasargent Logo"
              class="h-16 md:h-28 w-auto opacity-100"
            />
          </div>

          {/* Título - Perfectamente Centrado */}
          <h2
            id="modal-sucursal-name"
            class="text-4xl md:text-6xl font-black text-[#9e2a2b] text-center tracking-tighter w-full px-24"
          >
            Brasargent
          </h2>
        </div>
      </div>

      {/* Barra de Navegación Estilo PC - Dark Theme */}
      <div
        class="sticky top-0 z-10 bg-white/10 backdrop-blur-md border-b border-white/10 shadow-lg"
      >
        <div class="max-w-7xl mx-auto">
          <nav
            class="flex items-center justify-start overflow-x-auto whitespace-nowrap no-scrollbar scroll-smooth px-6 md:px-12 gap-2 md:gap-4"
            id="menu-nav"
            role="tablist"
          >
            {/* Botones de navegación dinámicos */}
          </nav>
        </div>
      </div>

      {/* Contenido del Modal - Dinámico */}
      <div
        class="max-h-[85vh] overflow-y-auto bg-transparent p-8 md:p-16 flex justify-center scroll-smooth no-scrollbar"
        id="menu-content"
      >
        <div class="max-w-5xl w-full" id="menu-items-container">
          {/* Aquí se renderizará el menú dinámicamente */}
        </div>
      </div>
    </div>
  </div>
</section>

<script>
// ============================================
// MODAL DE RESERVA - CON VALIDACIÓN DE HORARIOS
// ============================================

window.abrirModalReserva = function(sucursalId, sucursalNombre) {
    // Verificar si ya existe un modal abierto
    const existente = document.getElementById('modalReservaCliente');
    if (existente) {
        existente.remove();
    }
    
    const modalHTML = `
        <div class="modal-overlay" id="modalReservaCliente" onclick="window.cerrarModalReserva()">
            <div class="modal-container" onclick="event.stopPropagation()">
                <!-- Header -->
                <div class="modal-header">
                    <div class="modal-title">
                        <span class="icon">📅</span>
                        <div>
                            <h2>Reservar en ${sucursalNombre}</h2>
                            <p>Completa tus datos para solicitar una reserva</p>
                        </div>
                    </div>
                    <button class="modal-close" onclick="window.cerrarModalReserva()">✕</button>
                </div>
                
                <!-- Body -->
                <form id="formReservaCliente" class="modal-body">
                    <!-- Nombre -->
                    <div class="form-group">
                        <label>👤 Nombres Completos <span class="required">*</span></label>
                        <input type="text" id="nombreCliente" placeholder="Carlos Gómez Pérez" required />
                    </div>
                    
                    <!-- Teléfono con código de país -->
                    <div class="form-group">
                        <label>📱 Número Celular <span class="required">*</span></label>
                        <div class="telefono-wrapper">
                            <select id="codigoPais" class="codigo-pais">
                                <option value="591">🇧🇴 +591 (Bolivia)</option>
                                <option value="1">🇺🇸 +1 (EE.UU.)</option>
                                <option value="34">🇪🇸 +34 (España)</option>
                                <option value="52">🇲🇽 +52 (México)</option>
                                <option value="54">🇦🇷 +54 (Argentina)</option>
                                <option value="56">🇨🇱 +56 (Chile)</option>
                                <option value="57">🇨🇴 +57 (Colombia)</option>
                                <option value="58">🇻🇪 +58 (Venezuela)</option>
                            </select>
                            <input type="tel" id="telefonoCliente" placeholder="70000111" required />
                        </div>
                    </div>                    
                    <!-- Fecha -->
                    <div class="form-group">
                        <label>📅 Fecha <span class="required">*</span></label>
                        <input type="date" id="fechaReserva" required />
                        <span class="field-hint">Selecciona el día de tu reserva</span>
                    </div>
                    
                    <!-- Selector de Turno y Hora (UI Original Estática) -->
                    <div class="form-group" id="grupoTurnoHora">
                        <div id="turnosContainer" class="turnos-container">
                            <label class="field-label">
                                Turno
                                <span class="required">*</span>
                            </label>
                            <div id="turnosGrid" class="turnos-grid"></div>
                            <span class="error-message" id="errorTurno" style="display: none; color: #ef4444; font-size: 0.8rem; margin-top: 4px;">Por favor selecciona un turno</span>

                            <div id="horasWrapper" class="horas-wrapper hidden">
                                <div id="horasContainer" class="reloj-digital-wrapper">
                                    <div class="reloj-digital-container">
                                        <select id="relojHora" class="reloj-select"></select>
                                        <span class="reloj-colon">:</span>
                                        <select id="relojMinuto" class="reloj-select"></select>
                                    </div>
                                    <div id="relojInfo" class="reloj-info" style="display: none;"></div>
                                </div>
                                <span class="error-message" id="errorHora" style="display: none; color: #ef4444; font-size: 0.8rem; margin-top: 4px;">Por favor selecciona una hora</span>
                            </div>
                        </div>

                        <input type="hidden" id="turnoReserva" />
                        <input type="hidden" id="horaReserva" required />
                    </div>
                    
                    <!-- Personas (adultos) -->
                    <div class="form-group personas-section">
                        <label>👥 Número de Adultos <span class="required">*</span></label>
                        <div class="cantidad-wrapper">
                            <button type="button" class="btn-cantidad btn-menos" onclick="window._cambiarCantidad('personasReserva', -1, 1, 20)">−</button>
                            <input type="number" id="personasReserva" value="2" min="1" max="20" oninput="this.value = Math.max(1, Math.min(20, parseInt(this.value) || 2))" required />
                            <button type="button" class="btn-cantidad btn-mas" onclick="window._cambiarCantidad('personasReserva', 1, 1, 20)">+</button>
                            <span class="cantidad-label">adultos</span>
                        </div>
                    </div>
                    
                    <!-- 👶 SECCIÓN NIÑOS Y BEBÉS -->
                    <div class="form-section personas-section">
                        <p class="section-desc">Indica si vienes con niños para preparar todo con anticipación</p>
                        
                        <!-- Niños -->
                        <div class="form-group">
                            <label>👧 Niños (2-12 años)</label>
                            <div class="cantidad-wrapper">
                                <button type="button" class="btn-cantidad btn-menos" onclick="window._cambiarCantidad('cantidadNinos', -1)">−</button>
                                <input type="number" id="cantidadNinos" value="0" min="0" max="10" oninput="this.value = Math.max(0, Math.min(10, parseInt(this.value) || 0))" />
                                <button type="button" class="btn-cantidad btn-mas" onclick="window._cambiarCantidad('cantidadNinos', 1)">+</button>
                                <span class="cantidad-label">niños</span>
                            </div>
                        </div>
                        
                        <!-- Bebés -->
                        <div class="form-group">
                            <label>👶 Bebés (0-2 años)</label>
                            <div class="cantidad-wrapper">
                                <button type="button" class="btn-cantidad btn-menos" onclick="window._cambiarCantidad('cantidadBebes', -1)">−</button>
                                <input type="number" id="cantidadBebes" value="0" min="0" max="10" oninput="this.value = Math.max(0, Math.min(10, parseInt(this.value) || 0))" />
                                <button type="button" class="btn-cantidad btn-mas" onclick="window._cambiarCantidad('cantidadBebes', 1)">+</button>
                                <span class="cantidad-label">bebés</span>
                            </div>
                        </div>
                        
                        <!-- Opciones adicionales -->
                        <div class="checkbox-group">
                            <label class="checkbox-label">
                                <input type="checkbox" id="necesitaSilla" />
                                <span class="custom-checkbox"></span>
                                <div>
                                    <strong>🪑 Necesita silla para bebé</strong>
                                    <small>Prepararemos una silla alta</small>
                                </div>
                            </label>
                        </div>
                        
                        <div class="checkbox-group">
                            <label class="checkbox-label">
                                <input type="checkbox" id="necesitaMenuInfantil" />
                                <span class="custom-checkbox"></span>
                                <div>
                                    <strong>🍽️ Necesita menú infantil</strong>
                                    <small>Opciones especiales para niños</small>
                                </div>
                            </label>
                        </div>
                    </div>
                    
                    <!-- Comentarios -->
                    <div class="form-group">
                        <label>📝 Comentarios <span class="optional">(Opcional)</span></label>
                        <textarea id="observacionesReserva" rows="2" placeholder="Ej: Prefiero mesa cerca de la ventana"></textarea>
                    </div>
                </form>
                
                <!-- Footer -->
                <div class="modal-footer">
                    <button type="button" class="btn-cancel" onclick="window.cerrarModalReserva()">Cancelar</button>
                    <button type="submit" class="btn-save" id="btnEnviarReserva" form="formReservaCliente">
                        <span class="btn-text">📅 Solicitar Reserva</span>
                        <span class="btn-spinner hidden">⏳</span>
                    </button>
                </div>
            </div>
        </div>
    `;
    
    // Insertar modal en el DOM
    document.body.insertAdjacentHTML('beforeend', modalHTML);
    document.body.classList.add('modal-open');
    
    // Configurar fecha mínima de acuerdo al huso horario de Bolivia (en-CA garantiza formato YYYY-MM-DD sin bidi)
    const fechaInput = document.getElementById('fechaReserva');
    const hoyBolivia = new Intl.DateTimeFormat('en-CA', {
        timeZone: 'America/La_Paz',
        year: 'numeric',
        month: '2-digit',
        day: '2-digit'
    }).format(new Date());
    fechaInput.min = hoyBolivia;
    fechaInput.value = hoyBolivia;
    
    // Cargar turnos (estáticos en UI local) cuando cambie la fecha
    fechaInput.addEventListener('change', function() {
        window._cargarTurnosModal(sucursalId, this.value);
    });
    
    // Cargar turnos inmediatamente
    window._cargarTurnosModal(sucursalId, hoyBolivia);
    
    // Manejar envío
    document.getElementById('formReservaCliente').addEventListener('submit', async function(e) {
        e.preventDefault();
        await window.enviarReserva(sucursalId, sucursalNombre);
    });
}

window._cargarTurnosModal = async function(sucursalId, fecha) {
    const containerEl = document.getElementById('turnosContainer');
    const gridEl      = document.getElementById('turnosGrid');
    const horasWrap   = document.getElementById('horasWrapper');
    const turnoInput  = document.getElementById('turnoReserva');
    const horaInput   = document.getElementById('horaReserva');

    // Reset estado
    if (containerEl) containerEl.classList.remove('hidden');
    if (horasWrap) horasWrap.classList.add('hidden');
    if (gridEl) gridEl.innerHTML = '<div class="turnos-loading" style="padding: 1rem; text-align: center;"><div class="loading-spinner" style="border: 2px solid rgba(0,0,0,0.1); border-left-color: #9e2a2b; border-radius: 50%; width: 20px; height: 20px; animation: spin 1s linear infinite; display: inline-block;"></div></div>';
    
    const _relojH = document.getElementById('relojHora');
    const _relojM = document.getElementById('relojMinuto');
    if (_relojH) _relojH.innerHTML = '';
    if (_relojM) _relojM.innerHTML = '';
    
    if (turnoInput) turnoInput.value = '';
    if (horaInput) horaInput.value = '';

    try {
        const response = await fetch(`/api/reservas/horas-disponibles?sucursal_id=${sucursalId}&fecha=${fecha}`);
        const result = await response.json();
        
        if (!result.success || !result.data || !result.data.turnos) {
            if (gridEl) gridEl.innerHTML = '<span class="error-message" style="color: #ef4444; font-size: 0.9rem;">No se pudieron cargar los horarios para esta fecha.</span>';
            return;
        }

        const data = result.data;
        window._turnoData = data;

        const iconos = { manana: '☀️', tarde: '🌅', noche: '🌙', continuo: '🔁' };
        let html = '';
        
        if (data.turnos.length === 0) {
            html = '<span style="color: #666; font-size: 0.9rem; grid-column: 1 / -1; text-align: center; padding: 1rem;">No hay atención configurada en esta fecha.</span>';
        } else {
            data.turnos.forEach(turno => {
                const icono = iconos[turno.tipo] || '⏰';
                
                // Renderizamos la tarjeta limpia, sin bloqueos ni barras de capacidad
                html += `
                    <button
                        type="button"
                        class="turno-card"
                        data-turno-tipo="${turno.tipo}"
                        onclick="window._seleccionarTurnoModal(this)"
                    >
                        <span class="turno-icono">${icono}</span>
                        <div class="turno-info">
                            <span class="turno-nombre">${turno.label}</span>
                            <span class="turno-horario">${turno.inicio} – ${turno.fin}</span>
                        </div>
                    </button>
                `;
            });
        }

        if (gridEl) gridEl.innerHTML = html;
        
    } catch (error) {
        console.error('Error cargando turnos:', error);
        if (gridEl) gridEl.innerHTML = '<span class="error-message" style="color: #ef4444; font-size: 0.9rem;">Error de conexión. Intenta de nuevo.</span>';
    }
}

// Alias para mantener compatibilidad
window._cargarHorasModal = window._cargarTurnosModal;

// ============================================
// SELECCIONAR TURNO Y CARGAR RELOJ DIGITAL
// ============================================

window._seleccionarTurnoModal = function(element) {
    if (element.disabled || element.classList.contains('turno-card--disabled')) return;

    const gridEl      = document.getElementById('turnosGrid');
    const horasWrap   = document.getElementById('horasWrapper');
    const turnoInput  = document.getElementById('turnoReserva');
    const horaInput   = document.getElementById('horaReserva');
    
    const selectHora  = document.getElementById('relojHora');
    const selectMin   = document.getElementById('relojMinuto');
    const relojInfo   = document.getElementById('relojInfo');

    // Deseleccionar todos
    gridEl.querySelectorAll('.turno-card').forEach(c => c.classList.remove('turno-card--selected'));
    element.classList.add('turno-card--selected');

    const tipoTurno = element.dataset.turnoTipo;
    turnoInput.value = tipoTurno;

    // Ocultar error de turno
    const errorTurno = document.getElementById('errorTurno');
    if (errorTurno) errorTurno.style.display = 'none';

    // Limpiar hora seleccionada anterior
    horaInput.value = '';
    
    // Obtener horas del turno seleccionado
    const data = window._turnoData;
    if (!data) return;

    const turno = data.turnos.find(t => t.tipo === tipoTurno);
    if (!turno) return;

    // Extraer horas únicas (HH)
    const horasUnicas = [...new Set(turno.horas.map(h => h.hora.split(':')[0]))].sort();

    // Llenar select de horas
    selectHora.innerHTML = horasUnicas.map(h => `<option value="${h}">${h}</option>`).join('');

    // Función para actualizar minutos según la hora seleccionada
    function actualizarMinutos() {
        const horaSel = selectHora.value;
        const minutos = Array.from({length: 60}, (_, i) => String(i).padStart(2, '0'));

        selectMin.innerHTML = minutos.map(min => {
            return `<option value="${min}">${min}</option>`;
        }).join('');

        actualizarValorFinal();
    }

    // Función para actualizar el valor final de la hora y su cupo
    function actualizarValorFinal() {
        const horaSel = selectHora.value;
        const minSel = selectMin.value;
        if (!horaSel || !minSel) {
            horaInput.value = '';
            if (relojInfo) relojInfo.innerHTML = '';
            return;
        }

        const horaCompleta = `${horaSel}:${minSel}`;
        horaInput.value = horaCompleta;

        if (relojInfo) {
            relojInfo.innerHTML = '';
        }

        // Limpiar error de hora
        const errorHora = document.getElementById('errorHora');
        if (errorHora) errorHora.style.display = 'none';
    }

    // Bindear eventos
    selectHora.onchange = actualizarMinutos;
    selectMin.onchange = actualizarValorFinal;

    // Inicializar minutos e info
    actualizarMinutos();

    // Renderizar Info de capacidad general (fuera del grid de horas)
    let capInfo = horasWrap.querySelector('.capacidad-info');
    if (!capInfo) {
        capInfo = document.createElement('div');
        capInfo.className = 'capacidad-info';
        horasWrap.appendChild(capInfo);
    }
    capInfo.innerHTML = `
        <span class="info-disponibles">🪑 ${data.disponibles_hoy} cubiertos disponibles hoy</span>
        <span class="info-total">📋 Capacidad total: ${data.capacidad_total}</span>
    `;

    // Mostrar sección con animación
    horasWrap.classList.remove('hidden');
    horasWrap.classList.add('horas-animate-in');
    setTimeout(() => horasWrap.classList.remove('horas-animate-in'), 400);
}

// ============================================
// SELECCIONAR HORA (Mantenido por compatibilidad)
// ============================================

window._seleccionarHoraModal = function(element) {
    // Obsoleto por el nuevo Reloj Digital
}

// ============================================
// CAMBIAR CANTIDAD ADULTOS/NIÑOS/BEBÉS
// ============================================

window._cambiarCantidad = function(inputId, delta, min = 0, max = 10) {
    const input = document.getElementById(inputId);
    if (!input) return;
    let valor = parseInt(input.value) + delta;
    valor = Math.max(min, Math.min(max, valor));
    input.value = valor;
}

// ============================================
// ENVIAR RESERVA - CON NIÑOS, TURNO Y VALIDACIÓN DE HORA
// ============================================

window.enviarReserva = async function(sucursalId, sucursalNombre) {
    const btn = document.getElementById('btnEnviarReserva');
    btn.classList.add('loading');
    btn.disabled = true;
    
    const nombre = document.getElementById('nombreCliente').value.trim();
    const telefono = document.getElementById('telefonoCliente').value.trim();
    const codigoPais = document.getElementById('codigoPais').value;
    const fecha = document.getElementById('fechaReserva').value;
    const turno = document.getElementById('turnoReserva').value;
    const hora = document.getElementById('horaReserva').value;
    const personas = parseInt(document.getElementById('personasReserva').value);
    const observaciones = document.getElementById('observacionesReserva').value.trim();
    
    // 👶 Campos de niños
    const cantidadNinos = parseInt(document.getElementById('cantidadNinos')?.value) || 0;
    const cantidadBebes = parseInt(document.getElementById('cantidadBebes')?.value) || 0;
    const necesitaSilla = document.getElementById('necesitaSilla')?.checked || false;
    const necesitaMenuInfantil = document.getElementById('necesitaMenuInfantil')?.checked || false;
    
    // Validaciones en cliente
    if (!nombre || nombre.length < 3) {
        alert('❌ Por favor ingresa tu nombre completo');
        btn.classList.remove('loading'); btn.disabled = false;
        return;
    }
    
    if (!telefono || telefono.length < 7) {
        alert('❌ Por favor ingresa un número de teléfono válido');
        btn.classList.remove('loading'); btn.disabled = false;
        return;
    }
    
    if (!fecha) {
        alert('❌ Por favor selecciona una fecha');
        btn.classList.remove('loading'); btn.disabled = false;
        return;
    }

    if (!turno) {
        alert('❌ Por favor selecciona un turno (Mañana/Tarde/Noche)');
        btn.classList.remove('loading'); btn.disabled = false;
        return;
    }
    
    if (!hora) {
        alert('❌ Por favor selecciona una hora disponible de las opciones mostradas');
        btn.classList.remove('loading'); btn.disabled = false;
        return;
    }
    
    if (!personas || personas < 1) {
        alert('❌ Por favor ingresa el número de personas');
        btn.classList.remove('loading'); btn.disabled = false;
        return;
    }
    
    const data = {
        sucursal_id: sucursalId,
        nombre_cliente: nombre,
        telefono: telefono,
        codigo_pais: codigoPais,
        telefono_completo: `${codigoPais}${telefono}`,
        fecha: fecha,
        turno: turno,
        hora: hora,
        numero_personas: personas,
        cantidad_ninos: cantidadNinos,
        cantidad_bebes: cantidadBebes,
        necesita_silla_bebe: necesitaSilla,
        necesita_menu_infantil: necesitaMenuInfantil,
        observaciones: observaciones || null
    };
    
    try {
        const response = await fetch('/api/reservas/cliente', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(data)
        });
        
        const result = await response.json();
        
        if (result.success) {
            window.cerrarModalReserva();
            window.mostrarModalConfirmacion(sucursalNombre, nombre, fecha, hora, personas, cantidadNinos, cantidadBebes, necesitaSilla, necesitaMenuInfantil);
        } else {
            alert('❌ ' + (result.error || 'Error al solicitar la reserva'));
            btn.classList.remove('loading');
            btn.disabled = false;
        }
    } catch (error) {
        alert('❌ Error de conexión. Intenta de nuevo.');
        btn.classList.remove('loading');
        btn.disabled = false;
    }
}

// ============================================
// CERRAR MODAL
// ============================================

window.cerrarModalReserva = function() {
    const modal = document.getElementById('modalReservaCliente');
    if (modal) {
        modal.style.opacity = '0';
        setTimeout(() => {
            modal.remove();
            document.body.classList.remove('modal-open');
        }, 300);
    }
}

// ============================================
// MODAL DE CONFIRMACIÓN
// ============================================

window.mostrarModalConfirmacion = function(sucursalNombre, nombreCliente, fecha, hora, personas, ninos, bebes, silla, menuInf) {
    // Construir HTML de niños si aplica
    let ninosHTML = '';
    if (ninos > 0) {
        ninosHTML += `<div class="confirmacion-item"><span class="confirmacion-label">👧 Niños:</span><span class="confirmacion-valor">${ninos}</span></div>`;
    }
    if (bebes > 0) {
        ninosHTML += `<div class="confirmacion-item"><span class="confirmacion-label">👶 Bebés:</span><span class="confirmacion-valor">${bebes}</span></div>`;
    }
    let prepHTML = '';
    if (silla || menuInf) {
        let prep = [];
        if (silla) prep.push('🪑 Silla bebé');
        if (menuInf) prep.push('🍽️ Menú infantil');
        prepHTML = `<div class="confirmacion-item"><span class="confirmacion-label">📋 Preparación:</span><span class="confirmacion-valor">${prep.join(', ')}</span></div>`;
    }

    const modalHTML = `
        <div class="modal-overlay modal-confirmacion" id="modalConfirmacion" onclick="window.cerrarModalConfirmacion()">
            <div class="modal-container modal-confirmacion-container" onclick="event.stopPropagation()">
                <div class="modal-confirmacion-content">
                    <div class="confirmacion-icono">✅</div>
                    <h2>¡Reserva Solicitada!</h2>
                    <p>En breves momentos confirmaremos tu reserva en <strong>${sucursalNombre}</strong></p>
                    <p style="font-size: 0.9rem; color: #25d366;">
                        💬 Espera tu mensaje de confirmación por <strong>WhatsApp</strong>
                    </p>
                    <div class="confirmacion-detalles">
                        <div class="confirmacion-item">
                            <span class="confirmacion-label">Cliente:</span>
                            <span class="confirmacion-valor">${nombreCliente}</span>
                        </div>
                        <div class="confirmacion-item">
                            <span class="confirmacion-label">Fecha:</span>
                            <span class="confirmacion-valor">${new Date(fecha + 'T12:00:00').toLocaleDateString('es-ES')}</span>
                        </div>
                        <div class="confirmacion-item">
                            <span class="confirmacion-label">Hora:</span>
                            <span class="confirmacion-valor">${hora}</span>
                        </div>
                        <div class="confirmacion-item">
                            <span class="confirmacion-label">Adultos:</span>
                            <span class="confirmacion-valor">${personas || 2}</span>
                        </div>
                        ${ninosHTML}
                        ${prepHTML}
                    </div>
                    <button class="btn-confirmacion" onclick="window.cerrarModalConfirmacion()">
                        ¡Entendido!
                    </button>
                </div>
            </div>
        </div>
    `;
    
    document.body.insertAdjacentHTML('beforeend', modalHTML);
    document.body.classList.add('modal-open');
    
    setTimeout(() => {
        window.cerrarModalConfirmacion();
    }, 8000);
}

window.cerrarModalConfirmacion = function() {
    const modal = document.getElementById('modalConfirmacion');
    if (modal) {
        modal.style.opacity = '0';
        setTimeout(() => {
            modal.remove();
            document.body.classList.remove('modal-open');
        }, 300);
    }
}

// Cerrar con ESC
document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
        window.cerrarModalReserva();
        window.cerrarModalConfirmacion();
    }
});
</script>

<style is:global>
/* ============================================
   ESTILOS DEL MODAL (PÚBLICO)
   ============================================ */
.modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    padding: 20px;
    animation: fadeIn 0.3s ease;
}

.modal-container {
    background: #fffaf0;
    border-radius: 20px;
    max-width: 520px;
    width: 100%;
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    animation: slideUp 0.3s ease;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.modal-header {
    padding: 20px 24px 16px;
    border-bottom: 1px solid rgba(26, 26, 26, 0.06);
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.modal-title {
    display: flex;
    align-items: center;
    gap: 12px;
}

.modal-title .icon {
    font-size: 1.8rem;
}

.modal-title h2 {
    font-size: 1.3rem;
    font-weight: 700;
    color: #1a1a1a;
    margin: 0;
}

.modal-title p {
    color: rgba(26, 26, 26, 0.4);
    font-size: 0.85rem;
    margin: 2px 0 0;
}

.modal-close {
    background: transparent;
    border: none;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 1.2rem;
    color: #1a1a1a;
    transition: all 0.3s ease;
}

.modal-close:hover {
    background: rgba(26, 26, 26, 0.06);
}

.modal-body {
    padding: 20px 24px;
}

.form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
}

.form-group {
    margin-bottom: 16px;
}

.form-group label {
    display: block;
    font-weight: 500;
    color: #1a1a1a;
    font-size: 0.85rem;
    margin-bottom: 4px;
}

.form-group .required {
    color: #9e2a2b;
}

.form-group .optional {
    color: rgba(26, 26, 26, 0.3);
    font-weight: 400;
}

.form-group input,
.form-group select,
.form-group textarea {
    width: 100%;
    padding: 10px 14px;
    border: 1.5px solid rgba(26, 26, 26, 0.1);
    border-radius: 10px;
    font-size: 0.95rem;
    font-family: 'Inter', sans-serif;
    transition: all 0.3s ease;
    background: white;
    color: #1a1a1a;
}

.form-group input:focus,
.form-group textarea:focus {
    outline: none;
    border-color: #9e2a2b;
    box-shadow: 0 0 0 4px rgba(158, 42, 43, 0.08);
}

.telefono-wrapper {
    display: flex;
    gap: 8px;
}

.telefono-wrapper .codigo-pais {
    width: 120px;
    min-width: 120px;
    padding: 10px 12px;
    border: 1.5px solid rgba(26, 26, 26, 0.1);
    border-radius: 10px;
    font-size: 0.9rem;
    font-family: 'Inter', sans-serif;
    background: white;
    color: #1a1a1a;
    cursor: pointer;
}

.telefono-wrapper input {
    flex: 1;
}

.modal-footer {
    padding: 16px 24px 20px;
    border-top: 1px solid rgba(26, 26, 26, 0.06);
    display: flex;
    justify-content: flex-end;
    gap: 12px;
}

.btn-cancel,
.btn-save {
    padding: 10px 28px;
    border: none;
    border-radius: 10px;
    font-size: 0.95rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: 'Inter', sans-serif;
}

.btn-cancel {
    background: transparent;
    color: #1a1a1a;
    border: 1.5px solid rgba(26, 26, 26, 0.1);
}

.btn-cancel:hover {
    background: rgba(26, 26, 26, 0.04);
}

.btn-save {
    background: #9e2a2b;
    color: #fffaf0;
    box-shadow: 0 4px 16px rgba(158, 42, 43, 0.2);
}

.btn-save:hover {
    transform: scale(1.02);
    box-shadow: 0 6px 24px rgba(158, 42, 43, 0.3);
}

.btn-save.loading {
    opacity: 0.7;
    cursor: not-allowed;
}

.btn-save.loading .btn-text {
    display: none;
}

.btn-save.loading .btn-spinner {
    display: inline;
}

.btn-spinner {
    display: none;
}

/* Modal de Confirmación */
.modal-confirmacion-container {
    max-width: 420px;
}

.modal-confirmacion-content {
    padding: 32px 28px;
    text-align: center;
}

.confirmacion-icono {
    font-size: 4rem;
    margin-bottom: 12px;
    animation: bounceIn 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes bounceIn {
    0% { transform: scale(0); opacity: 0; }
    50% { transform: scale(1.2); opacity: 1; }
    70% { transform: scale(0.9); }
    100% { transform: scale(1); }
}

.modal-confirmacion-content h2 {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    color: #1a1a1a;
    margin: 0 0 8px;
}

.modal-confirmacion-content p {
    color: rgba(26, 26, 26, 0.6);
    font-size: 0.95rem;
    margin: 0 0 16px;
}

.confirmacion-detalles {
    background: rgba(255, 250, 240, 0.5);
    border-radius: 10px;
    padding: 12px 16px;
    margin: 12px 0 20px;
    text-align: left;
}

.confirmacion-item {
    display: flex;
    justify-content: space-between;
    padding: 4px 0;
    font-size: 0.9rem;
}

.confirmacion-label {
    color: rgba(26, 26, 26, 0.4);
}

.confirmacion-valor {
    color: #1a1a1a;
    font-weight: 500;
}

.btn-confirmacion {
    background: #9e2a2b;
    color: #fffaf0;
    border: none;
    padding: 12px 40px;
    border-radius: 10px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: 'Inter', sans-serif;
    width: 100%;
}

.btn-confirmacion:hover {
    transform: scale(1.02);
    box-shadow: 0 8px 30px rgba(158, 42, 43, 0.3);
}

/* Responsive */
@media (max-width: 480px) {
    .modal-container {
        margin: 10px;
        border-radius: 16px;
    }
    
    .form-row {
        grid-template-columns: 1fr;
        gap: 0;
    }
    
    .telefono-wrapper {
        flex-direction: column;
    }
    
    .telefono-wrapper .codigo-pais {
        width: 100%;
        min-width: unset;
    }
    
    .modal-footer {
        flex-direction: column-reverse;
    }
    
    .btn-cancel,
    .btn-save {
        width: 100%;
        text-align: center;
    }
}

/* ============================================
   HORAS DISPONIBLES - BOTONES DINÁMICOS
   ============================================ */
.horas-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(85px, 1fr));
    gap: 8px;
    margin-top: 8px;
    max-height: 220px;
    overflow-y: auto;
    padding: 4px;
}

.hora-option {
    padding: 10px 8px;
    border: 1.5px solid rgba(26, 26, 26, 0.08);
    border-radius: 10px;
    text-align: center;
    font-size: 0.85rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    background: white;
    color: #1a1a1a;
    font-family: 'Inter', sans-serif;
}

.hora-option:hover {
    border-color: #9e2a2b;
    background: rgba(158, 42, 43, 0.04);
    transform: scale(1.03);
}

.hora-option.selected {
    border-color: #9e2a2b;
    background: #9e2a2b;
    color: white;
    box-shadow: 0 4px 12px rgba(158, 42, 43, 0.3);
}

.hora-option .hora-disponibles {
    display: block;
    font-size: 0.6rem;
    color: rgba(26, 26, 26, 0.3);
    font-weight: 400;
    margin-top: 2px;
}

.hora-option.selected .hora-disponibles {
    color: rgba(255, 255, 255, 0.6);
}

.horas-loading {
    text-align: center;
    padding: 20px;
    color: rgba(26, 26, 26, 0.3);
    font-size: 0.85rem;
    grid-column: 1 / -1;
}

.horas-error {
    color: #ef4444;
    font-size: 0.85rem;
    text-align: center;
    padding: 16px;
    grid-column: 1 / -1;
    background: rgba(239, 68, 68, 0.04);
    border-radius: 10px;
    border: 1px solid rgba(239, 68, 68, 0.1);
}

.field-hint {
    display: block;
    color: rgba(26, 26, 26, 0.3);
    font-size: 0.75rem;
    margin-top: 4px;
}

@media (max-width: 480px) {
    .horas-grid {
        grid-template-columns: repeat(auto-fill, minmax(70px, 1fr));
        gap: 6px;
    }
}

/* ============================================
   CANTIDAD DE NIÑOS Y BEBÉS
   ============================================ */
.cantidad-wrapper {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 8px;
    margin-bottom: 8px;
}

.btn-cantidad {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 8px;
    font-size: 1.25rem;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.2s ease;
    border: none;
    user-select: none;
}

.btn-menos {
    background-color: #ef4444; /* Rojo */
    color: white;
}

.btn-menos:hover {
    background-color: #dc2626;
    transform: scale(1.05);
}

.btn-mas {
    background-color: #10b981; /* Verde */
    color: white;
}

.btn-mas:hover {
    background-color: #059669;
    transform: scale(1.05);
}

.cantidad-wrapper input[type="number"] {
    width: 70px !important;
    height: 40px !important;
    padding: 0 8px !important;
    text-align: center !important;
    border: 1.5px solid rgba(26, 26, 26, 0.1) !important;
    border-radius: 8px !important;
    font-size: 1rem !important;
    font-weight: 600 !important;
    background: white !important;
    color: #1a1a1a !important;
    -moz-appearance: textfield;
}

.cantidad-wrapper input[type="number"]::-webkit-outer-spin-button,
.cantidad-wrapper input[type="number"]::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
}

.cantidad-label {
    font-size: 0.9rem;
    color: rgba(26, 26, 26, 0.4);
    font-weight: 500;
    margin-left: 6px;
    text-transform: capitalize;
}

/* --- Estilos de Turnos --- */
.turnos-loading {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    padding: 16px;
    color: rgba(26, 26, 26, 0.4);
    font-size: 0.9rem;
}
.turnos-error {
    display: flex;
    align-items: center;
    gap: 8px;
    background: #fef2f2;
    border: 1px solid #fee2e2;
    color: #9e2a2b;
    padding: 12px 16px;
    border-radius: 12px;
    font-size: 0.9rem;
    margin-bottom: 12px;
}
.turnos-error-icon {
    font-size: 1.2rem;
}
.turnos-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-top: 8px;
    margin-bottom: 16px;
}
@media (min-width: 480px) {
    .turnos-grid {
        grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
    }
}
.turno-card {
    background: #ffffff;
    border: 1.5px solid rgba(26, 26, 26, 0.08);
    border-radius: 14px;
    padding: 14px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    cursor: pointer;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    outline: none;
}
.turno-card:hover:not(.turno-card--disabled) {
    transform: translateY(-2px);
    border-color: #9e2a2b;
    box-shadow: 0 6px 18px rgba(158, 42, 43, 0.08);
}
.turno-card--selected {
    border-color: #9e2a2b !important;
    background: rgba(158, 42, 43, 0.02) !important;
    box-shadow: 0 4px 16px rgba(158, 42, 43, 0.06);
}
.turno-card--selected::after {
    content: "✓";
    position: absolute;
    top: 6px;
    right: 8px;
    background: #9e2a2b;
    color: white;
    font-size: 0.7rem;
    font-weight: bold;
    width: 14px;
    height: 14px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
}
.turno-card--disabled {
    opacity: 0.45;
    cursor: not-allowed;
    background: #f9f9f9;
}
.turno-icono {
    font-size: 1.6rem;
    margin-bottom: 6px;
}
.turno-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
    margin-bottom: 8px;
}
.turno-nombre {
    font-weight: 700;
    font-size: 0.9rem;
    color: #1a1a1a;
}
.turno-horario {
    font-size: 0.75rem;
    color: rgba(26, 26, 26, 0.4);
}
.turno-badge {
    width: 100%;
    margin-top: auto;
}
.turno-pct-bar {
    width: 100%;
    height: 4px;
    background: rgba(26, 26, 26, 0.06);
    border-radius: 2px;
    overflow: hidden;
    margin-bottom: 4px;
}
.turno-pct-fill {
    height: 100%;
    background: #10b981;
    border-radius: 2px;
    transition: width 0.3s ease;
}
.turno-casi-lleno .turno-pct-fill {
    background: #f59e0b;
}
.turno-lleno .turno-pct-fill {
    background: #ef4444;
}
.turno-badge-label {
    font-size: 0.7rem;
    font-weight: 600;
}
.turno-disponible .turno-badge-label {
    color: #10b981;
}
.turno-casi-lleno .turno-badge-label {
    color: #f59e0b;
}
.turno-lleno .turno-badge-label {
    color: #ef4444;
}

/* --- Estilos de Reloj Digital y Capacidad --- */
.horas-wrapper {
    margin-top: 16px;
    border-top: 1px dashed rgba(26, 26, 26, 0.08);
    padding-top: 16px;
}
.reloj-digital-container {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    background: #fffdf9;
    border: 1.5px solid rgba(158, 42, 43, 0.15);
    border-radius: 16px;
    padding: 10px 16px;
    max-width: 240px;
    margin: 8px auto 12px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.02);
}
.reloj-select {
    font-size: 1.4rem !important;
    font-weight: 700 !important;
    border: 1px solid rgba(26, 26, 26, 0.15) !important;
    background: #ffffff !important;
    border-radius: 8px !important;
    padding: 6px 12px !important;
    color: #1a1a1a;
    cursor: pointer;
    outline: none !important;
    transition: all 0.2s ease;
}
.reloj-select:hover {
    border-color: #9e2a2b !important;
    background: rgba(158, 42, 43, 0.02) !important;
}
.reloj-select:focus {
    border-color: #9e2a2b !important;
    box-shadow: 0 0 0 3px rgba(158, 42, 43, 0.1) !important;
}
.reloj-select:focus {
    box-shadow: none !important;
    border: none !important;
}
.reloj-colon {
    font-size: 1.6rem;
    font-weight: 800;
    color: #9e2a2b;
    animation: blink 1s step-start infinite;
}
@keyframes blink {
    50% { opacity: 0; }
}
.capacidad-info {
    width: 100%;
    background: #fdfaf2;
    border: 1px solid rgba(245, 158, 11, 0.15);
    border-radius: 12px;
    padding: 10px 14px;
    display: flex;
    justify-content: space-around;
    gap: 12px;
    font-size: 0.85rem;
    margin-top: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.01);
}
.info-disponibles {
    font-weight: 700;
    color: #10b981;
}
.info-total {
    font-weight: 500;
    color: rgba(26, 26, 26, 0.5);
}

/* --- Reloj Info (disponibilidad debajo del reloj) --- */
.reloj-info {
    text-align: center;
    font-size: 0.85rem;
    padding: 8px 12px;
    border-radius: 10px;
    transition: all 0.3s ease;
    min-height: 20px;
}
.reloj-info.disponible {
    color: #10b981;
    background: rgba(16, 185, 129, 0.06);
    border: 1px solid rgba(16, 185, 129, 0.12);
}
.reloj-info.lleno {
    color: #ef4444;
    background: rgba(239, 68, 68, 0.06);
    border: 1px solid rgba(239, 68, 68, 0.12);
}

/* --- Loading Spinner --- */
.loading-spinner {
    width: 28px;
    height: 28px;
    border: 3px solid rgba(158, 42, 43, 0.15);
    border-top-color: #9e2a2b;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}
@keyframes spin {
    to { transform: rotate(360deg); }
}

/* --- Sección Niños/Bebés --- */
.form-section {
    margin-top: 8px;
    padding-top: 12px;
    border-top: 1px dashed rgba(26, 26, 26, 0.08);
}
.section-desc {
    font-size: 0.8rem;
    color: rgba(26, 26, 26, 0.4);
    margin: 0 0 12px;
    text-align: center;
}

/* --- Centrado de selectores de personas --- */
.personas-section {
    max-width: 320px;
    margin-left: auto;
    margin-right: auto;
}
.personas-section .cantidad-wrapper {
    justify-content: center;
}
.personas-section label {
    text-align: center;
}
</style>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const modal = document.getElementById("menu-modal");
    const modalSucursalName = document.getElementById("modal-sucursal-name");
    const menuContent = document.getElementById("menu-content");
    const menuItemsContainer = document.getElementById("menu-items-container");
    const menuNav = document.getElementById("menu-nav");
    const openButtons = document.querySelectorAll(
      '[data-modal-open="menu-modal"]',
    );
    const closeButtons = document.querySelectorAll(
      '[data-modal-close="menu-modal"]',
    );

    interface MenuItem {
      name: string;
      price: string;
      desc?: string;
    }

    interface MenuData {
      [section: string]: MenuItem[] | string | undefined;
      NOTAS?: string;
    }

    const MENUS: Record<string, MenuData> = {
      Churrasqueria: {
        ENTRADAS: [
          { name: "EMPANADA TUCUMANAS", price: "10" },
          { name: "CHORIZO PARRILLERO", price: "16" },
          { name: "MORCILLA TRADICIONAL", price: "16" },
          { name: "RIÑONES DE TERNERA", price: "40" },
          { name: "MOLLEJAS DE TERNERA", price: "50" },
          { name: "UBRE A LA PARRILLA", price: "40" },
        ],
        "CORTES CLÁSICOS": [
          { name: "BIFE DE CHORIZO", price: "" },
          { name: "ENTRAÑA CLÁSICA", price: "" },
          { name: "PUNTA DE “S”", price: "" },
          { name: "OJO DE BIFE", price: "" },
          { name: "ASADO DE TIRA CLÁSICO", price: "" },
          { name: "CUADRIL (Para 1 o 2 personas)", price: "" },
          {
            name: "BROCHETA MIXTA",
            price: "",
            desc: "Lomo de res, chorizo parrillero, morrones y cebollas asadas.",
          },
          {
            name: "BROCHETA DE RES",
            price: "",
            desc: "Lomo de res, morrones y cebollas asadas.",
          },
          {
            name: "MATAMBRE DE RES",
            price: "",
            desc: "Salsa a elección: barbacoa, chimichurri o roquefort.",
          },
          {
            name: "ESPECIAL PARA DOS",
            price: "",
            desc: "Punta de “S” junior, asado de tira, chorizo, ubre, morcilla y riñones de ternera.",
          },
          {
            name: "BABY BEEF",
            price: "",
            desc: "Con papas fritas (no incluye salad bar).",
          },
        ],
        "CORTES DRY AGED": [
          { name: "ASADO DE TIRA (10 HUESOS DRY AGED)", price: "" },
          {
            name: "T BONE DRY AGED",
            price: "",
            desc: "Corte con hueso en forma de T, que une al lomo y al lomillo.",
          },
          { name: "TOMAHAWK", price: "" },
        ],
        "CORTES ARGENTINOS": [
          { name: "BIFE DE CHORIZO", price: "" },
          { name: "ASADO DE TIRA", price: "" },
          { name: "OJO DE BIFE", price: "" },
          { name: "PUNTA DE “S”", price: "" },
          {
            name: "COLITA DE CUADRIL",
            price: "",
            desc: "Madurada 14 días a la parrilla con vainitas salteadas, puré de papa y chimichurri de la casa.",
          },
        ],
        "ESPECIAL DE LA CASA": [
          { name: "TRADICIÓN ARGENTINA (Costeleta)", price: "100" },
        ],
        "CORTES BRANGUS": [
          { name: "BIFE DE CHORIZO", price: "130" },
          { name: "ASADO DE TIRA", price: "125" },
          { name: "OJO DE BIFE", price: "130" },
          { name: "PUNTA DE “S”", price: "150" },
        ],
        CERDO: [
          { name: "MATAMBRITO DE CERDO AL CHIMICHURRI", price: "90" },
          { name: "MATAMBRITO DE CERDO A LA BBQ", price: "95" },
          { name: "COSTILLAS BBQ (1 Persona)", price: "100" },
        ],
        POLLO: [
          { name: "PECHUGA DE POLLO", price: "75" },
          { name: "MUSLO DESHUESADO", price: "65" },
        ],
        PESCADO: [
          {
            name: "SALMÓN GRILLADO",
            price: "155",
            desc: "Toque de mantequilla, ajo y romero, vegetales salteados, micro brócoli, mini berro, micro vegetales encurridos, aceite de hierbas y acompañado con salsa de queso roquefort.",
          },
          {
            name: "TRUCHA SALMONADA GRILLADA",
            price: "124",
            desc: "Acompañada con salsa emulsionada de alcaparras, mix de vegetales, micro albahaca, mini mostaza y aceite de carbón.",
          },
        ],
        ESPECIALIDADES: [
          {
            name: "LOMITO A LA PIMIENTA",
            price: "97",
            desc: "Medallones de lomo Black Angus a la parrilla, clásica salsa a la pimienta, acompañada de mix de vegetales, microgreens, arroz granado y papas fritas.",
          },
          {
            name: "LOMITO EN SALSA DE CHAMPIÑONES",
            price: "91",
            desc: "Lomo de res a la parrilla, salsa cremosa de champiñones frescos, vainitas salteadas y puré de papa.",
          },
          {
            name: "MILANESA DE TERNERA BRASARGENT",
            price: "110",
            desc: "(Al horno, libre de aceites) Lomito, salsa de tomate, queso mozzarella, queso catupiry, microgreens, pisca de pesto y tomates deshidratados.",
          },
          {
            name: "MILANESA DE TERNERA NAPOLITANA",
            price: "75",
            desc: "(Al horno, libre de aceites) Milanesa de lomito, jamón ahumado, salsa de tomate, queso mozzarella, microgreens.",
          },
        ],
        NOTAS:
          "TODOS LOS CORTES INCLUYEN BAR DE ENSALADAS Y 2 GUARNICIONES CALIENTES. AL COMPARTIR UN PLATO, TIENE UN COSTO ADICIONAL DE BS. 50 POR SALAD BAR.",
      },
      "Fast Grill": {
        ENTRADAS: [
          { name: "EMPANADAS TUCUMANAS", price: "10" },
          { name: "MIX DE ACHURAS (Ubre, Riñón y Chinchulí)", price: "59" },
        ],
        "CORTES CLÁSICOS": [
          { name: "PUNTA DE “S”", price: "85" },
          { name: "BIFE DE CHORIZO", price: "78" },
          { name: "OJO DE BIFE", price: "76" },
          { name: "CUADRIL (Para 1 persona)", price: "75" },
          { name: "CUADRIL (Para 2 personas)", price: "140" },
          { name: "ASADO DE TIRA", price: "75" },
          { name: "COSTELETA", price: "75" },
          { name: "PACUMUTO DE RES", price: "56" },
          { name: "PACUMUTO MIXTO", price: "54" },
          { name: "PECHUGA DESHUESADA", price: "52" },
          { name: "MUSLO DESHUESADO", price: "46" },
          { name: "MATAMBRITO DE CERDO", price: "68" },
          { name: "1/2 PACK DE COSTILLAS DE CERDO BBQ", price: "75" },
          {
            name: "PLATO VEGETARIANO",
            price: "45",
            desc: "Acompañado de 6 guarniciones.",
          },
        ],
        "PLATOS PREMIUM": [
          { name: "PICAÑA", price: "99" },
          { name: "OJO DE BIFE", price: "93" },
          { name: "BIFE DE CHORIZO", price: "87" },
          { name: "ENTRAÑA", price: "81" },
          { name: "TRADICIÓN ARGENTINA", price: "85" },
          { name: "PARRILLA MIXTA (P/DOS)", price: "180" },
          { name: "TOMAHAWK", price: "140" },
        ],
        "CORTES JUNIOR": [
          { name: "PUNTA DE “S” JUNIOR", price: "63" },
          { name: "CHURRASQUITO", price: "55" },
          { name: "BABY BEEF", price: "53" },
          { name: "ESPETINHO DE PICAÑA", price: "42" },
          { name: "ESPETINHO DE CUPIM (JIBA)", price: "42" },
          { name: "TOMAHAWK", price: "75" },
        ],
        EXTRAS: [
          { name: "CHORIZO", price: "10" },
          { name: "MORCILLA", price: "10" },
          { name: "GUARNICIÓN (FRIA O CALIENTE)", price: "15" },
        ],
        "DOMINGOS Y FERIADOS": [{ name: "SOPA DE MANÍ", price: "25" }],
        BEBIDAS: [
          { name: "GASEOSAS", price: "12" },
          { name: "JUGOS", price: "10" },
        ],
        POSTRES: [{ name: "PORCIÓN", price: "22" }],
        NOTAS:
          "TODOS LOS PLATOS CLÁSICOS Y PREMIUM VAN ACOMPAÑADOS DE 4 GUARNICIONES. LOS CORTES JUNIOR VIENEN ACOMPAÑADOS DE 3 GUARNICIONES.",
      },
      Rodizio: {
        "EL RODIZIO": [
          { name: "BUFFET DE ENSALADAS", price: "Incluido" },
          { name: "GUARNICIONES CALIENTES", price: "Incluido" },
          { name: "CORTES A LA ESPADA", price: "Incluido" },
          { name: "RODIZIO TRADICIONAL", price: "80" },
        ],
        NOTAS: "Precio por persona. No incluye bebidas ni postres.",
      },
    };

    const renderMenu = (sucursalName: string) => {
      const menu = MENUS[sucursalName] || MENUS["Churrasqueria"];
      if (!menuItemsContainer || !menuNav) return;

      // Renderizar Navegación
      let navHtml = "";
      let isFirst = true;
      for (const sectionTitle of Object.keys(menu)) {
        if (sectionTitle === "NOTAS") continue;
        const sectionId = sectionTitle.toLowerCase().replace(/\s+/g, "-");
        navHtml += `
          <button
            data-section="${sectionId}"
            class="menu-nav-btn group relative px-4 md:px-8 py-5 text-xs md:text-base font-black transition-all duration-300 flex-shrink-0 focus:outline-none tracking-[0.1em] md:tracking-[0.15em] uppercase ${isFirst ? "text-[#9e2a2b] border-b-4 border-[#9e2a2b]" : "text-gray-400 border-b-4 border-transparent hover:text-[#9e2a2b]"}"
            role="tab"
          >
            ${sectionTitle}
            <span
              class="absolute inset-x-0 bottom-[-4px] h-1 bg-[#9e2a2b] transform ${isFirst ? "scale-x-100" : "scale-x-0"} group-hover:scale-x-100 transition-transform duration-300"
            ></span>
          </button>
        `;
        isFirst = false;
      }
      menuNav.innerHTML = navHtml;

      // Re-vincular eventos de navegación
      const newNavButtons = menuNav.querySelectorAll(".menu-nav-btn");
      newNavButtons.forEach((button) => {
        button.addEventListener("click", () => {
          const sectionId = button.getAttribute("data-section");
          if (!sectionId || !menuContent) return;
          const section = document.getElementById(sectionId);
          if (section) {
            const offsetTop = section.offsetTop - menuContent.offsetTop - 20;
            menuContent.scrollTo({ top: offsetTop, behavior: "smooth" });
          }
        });
      });

      // Renderizar Contenido
      let html = '<div class="space-y-16">';

      for (const [sectionTitle, items] of Object.entries(menu)) {
        if (sectionTitle === "NOTAS" || typeof items === "string") continue;

        const sectionId = sectionTitle.toLowerCase().replace(/\s+/g, "-");
        html += `
          <div id="${sectionId}" class="menu-section scroll-mt-32">
            <h4 class="text-3xl font-black text-[#9e2a2b] mb-10 pb-4 border-b-2 border-[#9e2a2b]/20 tracking-tight flex items-center gap-4">
              <span class="w-2 h-8 bg-[#9e2a2b] rounded-full"></span>
              ${sectionTitle}
            </h4>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-16 gap-y-6">
              ${(items as MenuItem[])
                .map(
                  (item) => `
                <div class="group/item py-2 border-b border-gray-100 hover:bg-gray-50 transition-all duration-300 p-3 rounded-xl">
                  <div class="flex justify-between items-start gap-4">
                    <div class="flex-1">
                      <span class="text-gray-900 font-bold tracking-wide uppercase text-base block mb-1">
                        ${item.name}
                      </span>
                      ${item.desc ? `<p class="text-gray-500 text-sm font-medium leading-relaxed">${item.desc}</p>` : ""}
                    </div>
                    ${item.price ? `<span class="text-[#9e2a2b] font-black text-xl whitespace-nowrap">Bs.${item.price}</span>` : ""}
                  </div>
                </div>
              `,
                )
                .join("")}
            </div>
          </div>
        `;
      }

      if (menu.NOTAS) {
        html += `
          <div class="mt-20 mb-8 p-8 bg-gray-50 rounded-[32px] border border-gray-100 shadow-inner">
            <div class="flex items-start gap-4">
              <svg class="w-8 h-8 text-[#9e2a2b] shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <p class="text-base text-gray-600 font-bold leading-relaxed italic uppercase tracking-wider">
                ${menu.NOTAS}
              </p>
            </div>
          </div>
          <div class="h-12"></div>
        `;
      }

      html += "</div>";
      menuItemsContainer.innerHTML = html;

      // Re-vincular el observer después de renderizar
      const sections = document.querySelectorAll(".menu-section");
      sections.forEach((section) => observer.observe(section));
    };

    const updateActiveButton = (sectionId: string) => {
      const currentNavButtons = document.querySelectorAll(".menu-nav-btn");
      currentNavButtons.forEach((btn) => {
        const isActive = btn.getAttribute("data-section") === sectionId;
        const line = btn.querySelector("span");
        if (isActive) {
          btn.classList.add("text-[#9e2a2b]", "border-[#9e2a2b]");
          btn.classList.remove("text-gray-400", "border-transparent");
          if (line) line.classList.add("scale-x-100");
          if (line) line.classList.remove("scale-x-0");
        } else {
          btn.classList.remove("text-[#9e2a2b]", "border-[#9e2a2b]");
          btn.classList.add("text-gray-400", "border-transparent");
          if (line) line.classList.remove("scale-x-100");
          if (line) line.classList.add("scale-x-0");
        }
      });
    };

    const observerOptions = {
      root: menuContent,
      rootMargin: "-20% 0px -70% 0px",
      threshold: 0,
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const sectionId = entry.target.getAttribute("id");
          if (sectionId) updateActiveButton(sectionId);
        }
      });
    }, observerOptions);

    openButtons.forEach((button) => {
      button.addEventListener("click", () => {
        const sucursalName = button.getAttribute("data-sucursal-name");
        if (modalSucursalName && sucursalName) {
          modalSucursalName.textContent = sucursalName;
          renderMenu(sucursalName);
        }
        modal?.classList.remove("hidden");
        modal?.classList.add("flex");
        document.body.style.overflow = "hidden";
        if (menuContent) menuContent.scrollTop = 0;
      });
    });

    const closeModal = () => {
      modal?.classList.add("hidden");
      modal?.classList.remove("flex");
      document.body.style.overflow = "";
    };

    closeButtons.forEach((button) =>
      button.addEventListener("click", closeModal),
    );
    modal?.addEventListener("click", (e) => {
      if (e.target === modal) closeModal();
    });
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && !modal?.classList.contains("hidden"))
        closeModal();
    });
  });
</script>

<style>
  @keyframes gradient {   
    .modal-container-reserva {
        max-width: 100%;
        margin: 10px;
    }
}
</style>

<style>
  @keyframes gradient {
    0%,
    100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .animate-gradient {
    background-size: 200% auto;
    animation: gradient 3s ease infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 0.2;
    }
    50% {
      opacity: 0.3;
    }
  }

  @keyframes modal-enter {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-modal-enter {
    animation: modal-enter 0.3s ease-out;
  }

  /* Ocultar scrollbar en navegación */
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }

  /* Transición suave para botones de navegación */
  .menu-nav-btn {
    position: relative;
  }

  .menu-nav-btn::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: currentColor;
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .menu-nav-btn.active::after {
    transform: scaleX(1);
  }
</style>
